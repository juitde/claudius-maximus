## What and why

<!-- What does this change, and why does it look the way it does? If an
     alternative was considered and rejected, say so — that belongs here or in
     the commit messages, not only in your head. -->

## Checklist

- [ ] `gofmt -l .` reports nothing
- [ ] `go vet ./...` is clean
- [ ] `go test -race ./...` passes
- [ ] Each commit in this PR builds and passes tests on its own
- [ ] Commit messages explain *why*, not just *what* (see CONTRIBUTING.md)
